// test.cbuer

import "test";
import "test" as test;

namespace test{}

struct test
{
    int a;
    float b;
}

global::ptr malloc();

int main(int argc, char argv) {
    int64 x = 10;
    float y = 20.5;
    double z;
    char c = 'A';
    bool flag = true;
    string message = "Hello, C Buer!";
    
    if (x > 5) {
        y = y + 1.5 + y * 5;
    } else {
        y = y - 1.5;
    }
    
    for (int64 i = 0; i < x; local::i++++) {
        z = z + i;
    }
    
    while (z < 100) {
        z = z * 2;
    }

    // [a,b] = func();

    main(x, local::get_c()[1][2]);
    a.b.c[0]->a;
    return 0;
}

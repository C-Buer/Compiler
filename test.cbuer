// test.cbuer

ptr malloc(){}

int main(int argc, char argv) {
    int64 x = 10;
    float y = 20.5;
    double z;
    char c = 'A';
    bool flag = true;
    string message = "Hello, C Buer!";
    
    if (x > 5) {
        y = y + 1.5 + y * 5;
    } else {
        y = y - 1.5;
    }
    
    for (int64 i = 0; i < x; ++i) {
        z = z + i;
    }
    
    while (z < 100) {
        z = z * 2;
    }
    main(x, c);
    return 0;
}

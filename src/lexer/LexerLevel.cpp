#include "LexerLevel.hpp"

std::vector<Token> LexerLevel::processChunk(const SourceChunk &chunk)
{
    return {};
}

std::vector<Token> LexerLevel::processTokens(const std::vector<Token> &tokens)
{
    return tokens;
}
